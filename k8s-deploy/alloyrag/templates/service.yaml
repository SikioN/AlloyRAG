apiVersion: v1
kind: Service
metadata:
  name: {{ include "alloyrag.fullname" . }}
  labels:
    {{- include "alloyrag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.env.PORT }}
      protocol: TCP
      name: http
  selector:
    {{- include "alloyrag.selectorLabels" . | nindent 4 }}
